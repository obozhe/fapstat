export enum AdminPaths {
    Root = 'admin',
    Users = 'users',
    BanList = 'ban-list',
}

export enum AuthPaths {
    Root = 'auth',
    SignIn = 'sign-in',
    SignUp = 'sign-up',
    PasswordRestore = 'restore-password',
}
